import clsx from "clsx";
import Surah from "../Surah.tsx";

const Random = () => {

    return (
        <section className={clsx("bg-indigo-50/25 h-screen mt-20")}>
            <Surah/>
        </section>
    )
}

export default Random;